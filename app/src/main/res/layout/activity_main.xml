<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" android:layout_height="fill_parent"
    android:orientation="vertical"
    android:id="@+id/linearParent"
    android:padding="25dp"
    android:background="@color/warm">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Modul 3 - Autocomplete &amp; ListView"
        android:textSize="25dp"
        android:fontFamily="sans-serif-medium"
        android:padding="10dp"/>

    <AutoCompleteTextView
        android:id="@+id/dataautocomplete"
        android:padding="20dp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:completionThreshold="3"
        android:hint="AutoComplete - Ketikkan nama Program Studi" />

    <EditText android:id="@+id/datalistview"
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="pilihprodi"
        android:focusableInTouchMode="false"
        android:hint="ListView - Klik dan pilih nama Program Studi" />

</LinearLayout>